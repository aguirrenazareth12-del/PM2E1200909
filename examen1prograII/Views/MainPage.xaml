<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="examen1prograII.Views.MainPage"
    Title="Registro de Sitios">

    <ScrollView>

        <VerticalStackLayout
            Padding="20"
            Spacing="15">

            <Label
                Text="Registro de Sitios"
                FontSize="28"
                HorizontalOptions="Center"
                FontAttributes="Bold"/>

            <Frame
                BorderColor="#F8BBD0"
                CornerRadius="20">

                <VerticalStackLayout>

                    <Image
                        x:Name="imgSitio"
                        HeightRequest="250"
                        Aspect="AspectFill"/>

                </VerticalStackLayout>

            </Frame>

            <Entry
                x:Name="txtDescripcion"
                Placeholder="Ingrese una descripción"/>

            <Label
                x:Name="lblLatitud"
                Text="Latitud: "/>

            <Label
                x:Name="lblLongitud"
                Text="Longitud: "/>

            <Button
                Text="Tomar Foto"
                Clicked="TomarFoto"
                BackgroundColor="#F8BBD0"
                TextColor="Black"
                CornerRadius="15"/>

            <Button
                Text="Obtener Ubicación"
                Clicked="ObtenerUbicacion"
                BackgroundColor="#F8BBD0"
                TextColor="Black"
                CornerRadius="15"/>

            <Button
                Text="Guardar Sitio"
                Clicked="GuardarSitio"
                BackgroundColor="#F8BBD0"
                TextColor="Black"
                CornerRadius="15"/>

            <Button
                Text="Ver Lista"
                Clicked="IrLista"
                BackgroundColor="#F8BBD0"
                TextColor="Black"
                CornerRadius="15"/>

        </VerticalStackLayout>

    </ScrollView>

</ContentPage>